<h1>Textbooks:</h1>
<%=link_to "New Textbook", subjects_path %>


<% @textbooks.each do |textbook| %>
    Title: <%= textbook.title %>
    <%=link_to "Edit #{textbook.title}", textbook_path(textbook) %>
    <%= link_to "Add #{textbook.title}", textbook_path(textbook) %>
<% end%>

