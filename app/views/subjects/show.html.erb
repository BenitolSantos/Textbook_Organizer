 
  <%= @subject.name %>
  <%= form_for(@current_page) do |f| %>
    <%= f.hidden_field :user_id %>
    <%= f.hidden_field :textbook_id %>
    <%= f.submit "Change Page" %>
  <%end%>

 <% if @user.admin %>
        <%=link_to "Edit Subject", edit_subject_path(@subject) %>
  <% end %>


